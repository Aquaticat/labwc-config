# /etc/systemd/system/getty@tty1.service.d/autologin.conf
# DM-less boot: autologin on tty1; ~/.bash_profile then execs `uwsm start -- labwc`.
# On the host, replace `user` with your username.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin user --noclear %I $TERM

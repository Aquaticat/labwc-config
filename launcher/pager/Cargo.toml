[package]
name = "labwc-pager"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "ext-workspace-v1 pager backend for the sfwbar 3x3 grid: watch, activate, next, prev."

[dependencies]
wayland-client = "0.31"
wayland-protocols = { version = "0.32", features = ["client", "staging"] }

[lints]
workspace = true
